; Haskell tags.scm — tags queries for tree-sitter-haskell

(function (variable) @name) @definition.function

(data_type (name) @name) @definition.class

(class (name) @name) @definition.interface

(header (module) @name) @definition.module
