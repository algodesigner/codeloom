; CSS tags.scm — tags queries for tree-sitter-css

(rule_set
  (selectors
    (class_selector
      (class_name) @name)) @definition.method)

(rule_set
  (selectors
    (id_selector
      (id_name) @name)) @definition.method)
